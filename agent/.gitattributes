# Files executed or parsed inside Linux containers must stay LF even when
# the repo is checked out on Windows (core.autocrlf).
Dockerfile text eol=lf
.dockerignore text eol=lf
packaging/docker/** text eol=lf
